# Secrets and local config - never commit
test_secrets.json
**/test_secrets.json
appsettings.json
appsettings.*.local.json
appsettings.AIHub.json
appsettings.SIT.json
appsettings.ST.json
appsettings.QA.json
appsettings.SPP.json
appsettings.PP.json
appsettings.Production.json

# Build outputs
bin/
obj/
[Bb]uild/
[Oo]ut/
*.user
*.suo
*.cache
.vs/
*.rsuser

# Database (may contain test result URLs and run data)
app.db
*.db
*.db-shm
*.db-wal

# Build / IDE artifacts
build_errors.txt
build_output.txt
*.log
[Dd]ebug/
[Rr]elease/

# Scenarios folder - ignore all except README.md
Scenarios/*
!Scenarios/README.md

# Test data (CSVs may contain credentials per env)
TestData/

# Local reference and optional docs (do not push)
CHANGES_SINCE_LAST_COMMIT.md
ENVIRONMENT_SETUP.md
docs/

# Recorded test sessions (one-off)
recorded_test_*.feature
recorded_test_*.feature.cs

# User Secrets (stored outside repo)
secrets.json

# Playwright (browsers are installed per machine)
.playwright/

# OS
.DS_Store
Thumbs.db

# NuGet
*.nupkg
*.snupkg
project.lock.json
project.fragment.lock.json
artifacts/

# Test results and test project build
[Tt]est[Rr]esult*/
[Cc]overage/
*.trx
*.coverage
*.coveragexml
UiTestRunner.Tests/bin/
UiTestRunner.Tests/obj/

# Test run artifacts (screenshots/videos from runner)
wwwroot/screenshots/
wwwroot/videos/
